{
  "devDependencies": {
    "chalk": "^5.6.2",
    "concurrently": "^9.2.1",
    "globals": "^16.4.0",
    "inquirer": "^12.10.0",
    "lodash-es": "^4.17.21",
    "nodemon": "^3.1.10",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "pnpm build:buddy",
    "build:buddy": "pnpm --filter buddy build",
    "build:buddy:mac": "pnpm run build:buddy && pnpm --filter buddy build:mac",
    "dev": "clear && pnpm --filter buddy dev"
  },
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=23.0.0",
    "pnpm": ">=10.0.0"
  },
  "dependencies": {
    "source-map-support": "^0.5.21"
  },
  "type": "module"
}
